<config xmlns="http://tail-f.com/ns/config/1.0">
  <devices xmlns="http://tail-f.com/ns/ncs">
  <device>
    <name>{$NODE}</name>
      <config>
      <configuration xmlns="http://xml.juniper.net/xnm/1.1/xnm">
      <routing-instances>
        <instance>
          <name>{$VLAN}</name>
          <instance-type>vpls</instance-type>
          <interface>
            <name>{$INTERFACE}.{$VLAN}</name>
          </interface>
          <route-distinguisher>
            <rd-type>{$LOOPBACK}:{$VLAN}</rd-type>
          </route-distinguisher>
          <vrf-target>
            <community>target:65512:{$VLAN}</community>
          </vrf-target>
          <protocols>
            <vpls>
              <interface>
                <name>{$INTERFACE}.{$VLAN}</name>
              </interface>
              <no-tunnel-services/>
              <site>
                <name>{$DEVICE}</name>
                <site-identifier>{$PESI}</site-identifier>
                <interface>
                  <name>{$INTERFACE}.{$VLAN}</name>
                </interface>
              </site>
            </vpls>
          </protocols>
        </instance>
      </routing-instances>
      <interfaces>
      <interface>
        <name>{$INTERFACE}</name>
          <unit>
            <name>{$VLAN}</name>
            <encapsulation>vlan-vpls</encapsulation>
            <vlan-id>{$VLAN}</vlan-id>
          </unit>
      </interface>
      </interfaces>
      </configuration>
      </config>
  </device>
  </devices>
</config>
